/**
 * 定義標籤物件內容。
 * @typedef {Object} TagObj
 * @property {number} id 標籤 ID。
 * @property {string} name 標籤名稱。
 */

/**
 * 定義 router 中 route 的 meta 內容。
 * @typedef {Object} RouteMeta
 * @property {string|function} [title] - 頁面標題（支援函數動態標題）
 * @property {boolean} [isRequiresLogin] - 是否需要登入權限
 */
